{% set today = now|date('Y-m-d H:i') %}


{% for entry in entries %}
    {% if entry.salesEndDate|date('Y-m-d H:i') >= today %}
        {{ include('_components/nft_card')}}
    {% endif %}
{% endfor %}

